import React from 'react';
import PropTypes from 'prop-types';

const Main = props => {
     return (
          <div>
               <p>Hello Ridoy</p>
          </div>
     );
};

Main.propTypes = {
     
};

export default Main;